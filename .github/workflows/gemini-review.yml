name: '🔎 Gemini Code Review'

on:
  pull_request:
    branches:
      - main
    paths:
      - 'agent/**'
      - 'bff/**'
      - 'frontend/**'
      - 'tests/**'
      - 'pyproject.toml'

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Run Gemini Code Review
        uses: sshnaidm/gemini-code-review-action@v2
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        with:
          gemini-key: ${{ secrets.GEMINI_API_KEY }}
          model: 'gemini-3.5-flash'
          add-files: 'false'
          context-lines: '30'
          prompt: 'Review the code changes and provide all feedback and comments exclusively in English (UK).'
